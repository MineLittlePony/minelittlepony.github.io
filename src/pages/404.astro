---
import { Image } from 'astro:assets'
import IMAGE from '~/assets/derpibooru/376307.jpg'
import RootLayout from '~/layouts/root/RootLayout.astro'

const derpibooruId = 376307
---

<RootLayout
  title="Not Found"
  description="Requested resource is not found. Please, check the url or the visit the home page."
  heading="Requested page is not found"
  class="items-center justify-center"
>
  <a href={`https://derpibooru.org/images/${derpibooruId}`} title={`Derpibooru: ${derpibooruId}`}>
    <Image class="max-h-[50svh] w-auto" src={IMAGE} alt="The page is lost" />
  </a>
</RootLayout>
