---
export interface Props {
  id: number
  src: string
  alt: string
}

const { id, src, alt } = Astro.props
---

<a href={`https://derpibooru.org/images/${id}`} title={`Derpibooru: ${id}`}>
  <img class="max-h-[50svh] w-auto" src={src} alt={alt} />
</a>
